# SEO fix plan — ask for these one per session, in this order

Written 2026-09-02 after the structure review. Tick items off here as they ship.
Background: marketing/seo/keyword-map.md (demand + priorities) and the memory
note gamesbazaar-seo-fix-plan.

| # | Ask for | What it does | Why first | Size | Status |
|---|---|---|---|---|---|
| 1 | "Build the listing lifecycle rule" (replaces the old #1 and #2) | Per listing, the site decides pause vs goodbye. **Pause** (switched off recently, no permanent signal, e.g. Fazer quote missing): page stays live as out-of-stock — same URL/title/schema (availability OutOfStock), no buy button, sibling options shown, out of the sitemap — for at most ~30 days. **Goodbye** (game gone from Fazer, Pakistan region gone, edition discontinued, hand-retired, superseded by a renamed twin, or the 30 days ran out): 308 to the heir = the active twin (same game + edition + method + region) → else the category page → else the section page. **Deleted outright**: same heir logic, recorded at delete time (RetiredListing table; migrate today's 1,411-id JSON map into it; seed the 14 hard-deleted top pages: 30115, 30120, 30157, 30158 = FC 27 editions per Shayan, plus 19571, 11886, 9744, 12264, 11891, 9442, 190, 8655, 12723, 21146 via Wayback). **Never indexed** (active only a few days): plain 404. Shayan's objection 2026-09-02: many inactive listings are PERMANENTLY gone (Fazer renames → twins, editions discontinued) — never show those as out of stock. | 229 inactive + every future deletion currently 404 with no trace; 712 imp on 19 dead top pages | backend (detail view, RetiredListing model + migration, delete hook, sync reason → permanent flag) + frontend listing page, full runbook deploy | **SHIPPED 2026-09-02 (de3d384)** — `core/listing_lifecycle.py`, migration 0080, `import_retired_listings` + `listing_lifecycle` commands; prod after backfill: 8 paused, 188 redirecting, 34 → 404, 1,425 deleted ids redirecting; IndexNow accepted 236 listing URLs; "never indexed" = buyable under 1 day (a 3-day-old listing already ranked); 30157/30158 heir = Dragon Ball accounts by neighbour evidence, not FC 27 (re-pin in admin › Retired listings if wrong) |
| 2 | "Add breadcrumb links and section links to listing pages" | Breadcrumb Game › Category become real links + BreadcrumbList JSON-LD; header/footer carry /keys, /gift-cards, /accounts, /rentals site-wide. | 6,820 product pages currently pass nothing to the 138 category pages | backend (two read-only slug fields on the listing API, no migration) + frontend, 1 deploy: build → restart gamesbazaar-web → restart frontend | **SHIPPED 2026-09-02 (bc1effb)** — full deploy (build → check → restart web → restart frontend), then the Next fetch cache was cleared so the first crawl of every listing gets the links; listing API now sends `game_slug`/`category_slug` (buyer-facing slug); visible trail Home › Game › Category as real links (`lib/listingSeo.listingBreadcrumbs`); BreadcrumbList JSON-LD = Home › "<Game> <Category>" (category page URL) › listing (`listingSchemaBreadcrumbs`) — the game item is left out because /games/<slug> always redirects (Shayan's review note); header strip `components/SectionNav.js` (fixed 41px, in the server HTML) + footer Shop column share `lib/sectionNav.js` (Keys, Accounts, Gift Cards, Subscriptions, Rentals); CLS probe on the production build 2026-09-02: 0 shift on every click-through at 1366 and 390 wide, strip never a shift source; pre-fix cached payloads fall back to plain-text crumbs and no BreadcrumbList |
| 3 | "Remove the marketplace seller signals from product pages" | Product schema seller → Organization "GamesBazaar"; meta description drops "sold by GamesbazaarStore"; /seller/<name> pages noindex (or the link points at /about). | Google/AI read "Person sells on marketplace" today | frontend only, 1 deploy | **SHIPPED 2026-09-03 (ede734f)** — Quick Frontend-Only Deploy (pull → build → restart frontend), verified live: listing/9 seller = Organization, description without "sold by", /seller/GamesbazaarStore = `noindex, follow`, home Organization carries the `@id` after its 60 s ISR refresh; no cache clearing needed; `lib/seo.productJsonLd` seller = Organization GamesBazaar (`@id` `<site>/#organization`, shared with the root-layout Organization, which gained the same `@id`); listing meta description dropped " sold by <name>"; `/seller/<name>` metadata = `noindex, follow` (page + link kept: sellers may return, dashboard links there); visible seller card on listings untouched; tests updated (`test/seo.test.js`), 172 pass |
| 4 | "Clean the account listing titles for SEO" | Extend the offer-mode title cleaner to standard listings: strip "\| STEAM \|", "0H Played", "Can Change Data", "Fast Delivery" from title/H1/schema name; DB titles untouched. | 46 of 47 account titles start with a pipe | frontend lib/listingSeo.js, 1 deploy | **SHIPPED 2026-09-03 (b23df76)** — Quick Frontend-Only Deploy (pull → build → restart frontend), verified live: listing/9018 = "ELDEN RING (PC) Steam Account" in <title>, H1, description, Product name and breadcrumb; listing/36 = "Anno 1800 (PC) Ubisoft Account"; keys (9014) and gift cards (14122) unchanged; IndexNow accepted all 916 account URLs. `standardListingName` in `lib/listingSeo.js`: drops the segments Full Access / Fast Delivery / Can Change Data / Email + Password Changeable / 0H Played (also when trailing without a pipe), folds a leading launcher tag into the product word (STEAM → "Steam Account", UBISOFT → "Ubisoft Account", EPIC → "Epic Games Account"), strips emoji, never repeats words the title has, returns untouched titles byte-for-byte; DB titles, category tiles, checkout line and analytics still show the raw title. Dry run on all 916 live titles: 915 changed, titles fitting the 60-char budget with the site name 1 → 197. "Full Access" stripped too (on 907/907 templated titles = boilerplate). |
| 5 | "Evaluate the price-in-title pilot" — on or after 2026-09-26 | Compare the 12 pilot pages' CTR/position against the 08-29 baseline. If it wins, put the PKR price into the DEFAULT keys/accounts title template (one code change, 300+ pages). If not, revert the token. | Every "hold" row in the keyword map waits on this | analysis + maybe 1 backend change | ENDED EARLY 2026-09-03 (Shayan: "fix it all now") — pilot rolled out as the DEFAULT title for every keys/accounts page without hand-written copy (backend default_seo_title); LIVE 2026-09-03 (8ca5c60) |
| 5b | "Redirect the duplicate URLs of renamed category pages" | Renamed pages answer 200 with a self-canonical at BOTH the buyer-facing slug and the underlying category slug: /games/playstation/subscription + /subscriptions, /games/xbox/game-pass + /subscriptions, /games/roblox/robux + /currency (found 2026-09-03 after fix #2; more may exist — check every page with a display slug). Fix: category page 308s the underlying slug to the display slug when they differ (API already returns the display slug). | Split signals on three money pages | frontend category page.js, 1 deploy | built 2026-09-03 (category page 308s the underlying slug to the buyer-facing one); LIVE 2026-09-03 (8ca5c60) |
| 6 | "Work the keyword map" (repeat monthly) | Do the **work** rows: PUBG UC title + "1 UC in PKR" FAQ after the redirect recrawl (~09-20); Yalla Live title → "gift cards"; Cricket 26 title + copy (+ stock the keys page); then whatever the pilot unlocks. | The demand-ordered backlog | seo_copy.json edits, seed, no build | first pass built 2026-09-03: PUBG UC title + 1-UC/Easypaisa FAQs, Yalla Live title, Cricket 26 copy, PSN/Steam/Razer Gold/Game Pass/PS Plus/Robux titles, Valorant + Minecraft FAQs, FC 27 keys+accounts + BeamNG bodies; LIVE 2026-09-03 (8ca5c60, seeded, IndexNow 1,199 URLs). Repeat monthly after the October refresh |
| 7 | "Refresh the keyword map" — first week of October | New GSC export into marketing/seo/gsc-<date>/, regenerate families, compare trend, ask the 10 AI questions, log citations. | Closes the loop | analysis only | open, monthly |
| 8 | Optional later: "Canonicalize gift-card variant listings to their category page" | Denomination×region variant pages point canonical at the offer-mode category page. | Variants earn little, add crawl weight | frontend, 1 deploy | parked |
| 10 | "Build the gift-card region pages" | Allow-listed brand+region pages at `/games/<brand>/gift-cards/<region>` (≈25: PSN USA/UK/UAE/Türkiye/India/Saudi; Steam USA/Türkiye/India/UAE/Global; Xbox USA/UK/Türkiye/Europe; Valorant VP Türkiye/USA/Europe/India; Google Play USA/Türkiye/India/UK/Saudi; iTunes USA/Türkiye once its slug is found). Each = brand page with the region filter fixed: own priced title (cheapest in that region), H1, short copy + region FAQ (seo_copy keyed by region), self-canonical, sitemap, breadcrumb Brand › Region, "shop by region" row on the brand page. `?region=` URL keeps canonicalizing to the brand page; empty region → auto-noindex; IndexNow the new URLs. Brand page, variant pages, syncs, checkout, delivery all unchanged. NOT the full brand×region matrix (500+ thin pages). | Competitors rank for "psn gift card usa price in pakistan" with exactly these pages (steamshop.pk/psn-gift-card-us, gameforce, venturegames, shopon); PK autocomplete shows psn us/uk/india/uae/turkey, xbox turkey, valorant turkey/eu, itunes usa/turkey; we have 0 visibility (4 region-word queries, 7 imp) | backend allow-list + migration + API + sitemap, frontend route reusing GameCategoryClient, tests, full runbook deploy | **SHIPPED 2026-09-03 (ce96d2b)** — full runbook deploy (pull → build → migrate 0081 → restart web + frontend) → `seed_seo_text` on the server ("26 region page(s) created", 0 dead links) → IndexNow accepted 32 URLs (26 region + 6 brand pages, HTTP 200). Live-verified: `/games/playstation/gift-cards/usa` = "PSN Gift Card USA Price in Pakistan from PKR 300", self-canonical, index/follow, H1 "PlayStation Gift Cards (USA)", crumb Home › PlayStation Gift Cards › USA, region row, BreadcrumbList + FAQPage; same on steam/global and valorant/turkiye; brand page title/canonical/H1 unchanged + row added; `/gift-cards/japan` 404. Allow-list = `CategoryRegionPage` rows created by `seed_seo_text` from `seo_copy.json` entries with a `region` key (26: PSN 6, Steam 5, Xbox 4, Valorant VP 4, Google Play 5, iTunes 2 — iTunes slug is `app-store-itunes`); empty region → noindex + out of sitemap/row automatically; copy has NO hard-written denominations (Shayan's rule: the smallest card in stock is in the title, values live on the tiles). Sitemap picks the URLs up on its hourly refresh. Search Console indexing requests = on Shayan |
| 9 | Authority (on Shayan, no code): record the how-to videos; PSN card + Steam wallet videos are the next scripts after PUBG | Only thing that moves page-2 money pages | — | on Shayan |

Rules while working through this: leave a changed page alone 4 weeks; (pilot hold lifted 2026-09-03); never propose bringing top-ups back; commit only
after Shayan reviews; every deploy = Quick Frontend-Only Deploy unless a
migration is involved (then full runbook order: build → migrate → restart).
