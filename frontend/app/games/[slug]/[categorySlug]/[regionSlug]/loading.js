// The category skeleton, mounted below this segment's layout so a
// notFound() in that layout still answers with a real 404 (see
// ../CategorySkeleton.js).
export { default } from '../CategorySkeleton';
